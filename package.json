{
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter ./packages run build",
    "lint": "eslint **/*.ts",
		"test": "pnpm -r --filter ./packages run test"
  },
  "devDependencies": {
    "@innocenzi/eslint-config": "^0.4.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "eslint": "^7.32.0",
    "@types/node": "^16.10.8",
    "typescript": "^4.4.4"
  },
	"pnpm": {
		"overrides": {
			"eslint": "^7.32.0"
		}
	}
}
